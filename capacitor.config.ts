import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.forexsense.ai',
  appName: 'ForexSense AI',
  webDir: 'dist',
  server: { androidScheme: 'https' }
};

export default config;
